

function MapPage(props) {

    const mapOptions = {
        zoom: 10,
        center: {
            lat: setSelected({ lat }),
            lng: setSelected({ lng }),
        },
    };

    function Test() {

        return (
        
    );
    }
