import React, { useEffect, useState } from "react";
import CREATE_IN_TRIP from "../../../TestingDatabase/GraphQL/inserts.js";

const Save_trip_button = (props) => {

    return <button/>
}

export default Save_trip_button;